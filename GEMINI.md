# Zombie2 Project Implementation Record

이 문서는 프로젝트의 기능 구현 역사와 설정 사항을 기록하는 공식 문서입니다.

## 1. 프로젝트 설정 및 인프라
- **저장소 관리:** 
  - 로컬 Git 저장소 초기화 및 GitHub 연동 (`origin/main`).
  - Unity 프로젝트 전용 `.gitignore` 파일을 최상위에 생성하여 불필요한 바이너리(Library, Temp 등)를 추적에서 제외.
  - **대용량 파일 관리:** GitHub의 100MB 제한을 초과하는 대용량 클래식 음악 파일(`Assets/Gregor Quendel...`)을 `.gitignore`에 추가하여 백업 안정성 확보.

## 2. 시각 효과 및 URP 최적화
- **셰이더 일괄 수정:** URP(Universal Render Pipeline) 환경에서 에셋들이 보라색으로 깨져 보이던 문제를 해결.
- **주요 수정 대상:**
  - `Explosion` 이펙트 관련 머티리얼 4종.
  - `IceMagicEF` 이펙트 관련 머티리얼 15종.
  - `SnowMan` 눈사람 프리팹 관련 머티리얼 4종 (Body, Head, Eye, Nose).
- **적용 사항:** 기존 레거시 셰이더를 `Universal Render Pipeline/Lit` 및 `Particles/Unlit`으로 전면 교체 및 텍스처 매핑 최적화.

## 3. 핵심 게임 시스템 로직
### 수류탄 (ThrowableGrenade)
- **데미지 면역:** 폭발 시 플레이어 캐릭터는 데미지를 입지 않도록 태그 기반 면역 로직 추가.
- **사운드 최적화:** 폭발 소리가 거리나 3D 공간 제약 없이 선명하게 들리도록 2D 강제 재생 로직 적용.
- **생명주기 관리:** 폭발 이펙트가 무한 반복되지 않도록 생성 2초 후 자동 삭제 처리.
- **밸런스 조정:** 폭발 반경을 3m로 최적화.

### 축음기 유인 시스템 (Gramophone Lure)
- **습득:** 플레이어가 닿으면 인벤토리에 추가되며 점수 획득.
- **설치:** `F` 키 입력 시 즉시 설치 (설치 쿨타임 제거).
- **좀비 AI 연동:** 좀비가 플레이어보다 설치된 축음기(반경 15m)를 최우선 추적하도록 AI 행동 트리 고도화 (10초 유지).

### 눈사람 AI 동료 (SnowMan Companion)
- **소환:** `T` 키로 소환하며 30초의 쿨타임 적용.
- **UI 동기화:** 소환 시 UI 아이콘이 흐려지며 원형 쿨타임(Radial Fill)이 차오르는 시각적 피드백 구현.
- **지능형 이동:** **NavMesh**를 사용하여 장애물을 우회하며 플레이어 오른쪽 0.5m 거리를 정확히 유지. (소환 위치 최적화로 팝업 현상 제거)
- **구르기 연출:** 이동 거리에 비례하여 몸통(Mesh)만 물리적으로 회전하고 머리는 똑바로 유지되는 특수 연출.
- **전투 능력:**
  - **슬로우 오라:** 정면 1.5m 지점에 지름 1m의 원형 범위를 형성하여 좀비를 50% 느리게 만듦.
  - **시각 효과:** 슬로우 존에 `IceMagicEF` 마법진 효과를 공중에 띄워 범위 시인성 강화.
  - **바닥 표시기(Ground Indicator):** 실제 슬로우가 적용되는 영역을 바닥에 Quad 메쉬로 표시.
    - **지형 적응:** 레이캐스트(Raycast)를 사용하여 언덕이나 경사면에서도 지면 높이를 실시간 감지하여 표시기가 묻히지 않음.
    - **충돌 방지:** 표시기 프리팹의 콜라이더를 자동으로 비활성화하여 플레이어 이동 방해 요소 제거.
    - **정밀 동기화:** 시각적 원의 크기와 물리적 판정 범위를 수학적으로 100% 일치시킴.
  - **자폭:** 20초 뒤 강력한 폭발 데미지(300)와 반경(3m)을 가지며 소멸.
  - **다중 폭발 효과:** 자폭 시 `Icicle Bumb`과 `Ice Hit` 효과가 동시에 발생하도록 연출 강화.

## 4. 좀비 시스템 및 신규 개체
- **피격 효과 개선:** 총기 공격 시 혈흔 효과(`bloodSprayEffect`)가 보이지 않던 문제를 해결하기 위해 `Instantiate` 방식으로 로직을 전면 개편했습니다.
- **다중 타격 연출:** 이제 좀비를 연속해서 맞추더라도 각각의 피격 지점에서 개별적으로 혈흔 파티클이 생성되어 훨씬 더 역동적이고 타격감 있는 연출이 가능합니다.
- **핑크 분열 좀비 (Zombie Pink):**
  - 한 발만 맞아도 3마리의 작은 좀비로 분열하여 공격하는 특수 개체를 추가했습니다.
  - 분열된 작은 좀비들은 크기가 0.5배이며, 본체의 공격력과 체력을 계승합니다.

## 5. 지형 및 환경 시스템
- **점진적 설원화 (Progressive Whitening):**
  - 게임이 진행됨에 따라(1~7 웨이브) 바닥의 `Grass` 머티리얼 색상이 점점 변하는 시스템을 구현했습니다.
  - 1웨이브에서는 고정된 시작 색상(R:138, G:181, B:73)으로 강제 초기화되어 항상 초록색으로 시작합니다. (URP `_BaseColor` 속성 사용)
  - 7웨이브에 도달하면 지정된 설원 색상(R:220, G:239, B:253)으로 완전히 변합니다.
  - `ZombieSpawner`의 `Start()` 시점에 머티리얼 색상을 초기화하여 에셋 저장 상태와 상관없이 일관된 시각적 경험을 제공합니다.
  - `Color.Lerp`를 사용하여 각 웨이브가 시작될 때마다 부드럽게 색상이 보간됩니다.
- **랜덤 어두운 웨이브 연출:**
  - 2~7 웨이브 중 무작위로 2개의 웨이브를 선택하여 조명 밝기(`Directional Light`)를 낮추는(0.2) 시스템을 구현했습니다.
  - 웨이브가 시작될 때 밝기가 서서히 변하도록 코루틴을 사용하여 시각적인 몰입감을 더했습니다.
- **인트로 씬 색상 고정:**
  - `IntroSceneController`를 통해 인트로 씬이 시작될 때마다 바닥 머티리얼 색상을 항상 고정된 초록색(R:138, G:181, B:73)으로 리셋합니다. (URP `_BaseColor` 속성 사용)
  - 이를 통해 메인 게임에서 하얗게 변한 바닥이 인트로 씬에 영향을 주지 않도록 보장합니다.
- **게임 종료 기능 및 버튼 클릭 간섭 수정:**
  - 인트로 씬에 추가된 `Exit Button`을 위해 `ExitGame` 기능을 구현하고, `Start Button`과 `Exit Button`이 정상적으로 작동하도록 이벤트를 설정했습니다.
  - **클릭 차단 버그 수정:** 인트로 씬의 전체 화면을 덮고 있던 `FadeImg`(페이드용 이미지)의 `Raycast Target` 속성이 켜져 있어 마우스 클릭을 가로막아 버튼이 눌리지 않던 현상을 해결했습니다. `IntroSceneController` 시작 시 `FadeImg` 객체의 `raycastTarget`을 즉시 비활성화하고, 2초 후 오브젝트 자체를 꺼지도록 코루틴 처리를 적용하여 연출 보장과 클릭 정상화를 모두 달성했습니다.

## 6. 최종 웨이브 연출 및 승리 조건 (Final Sequence)
- **7웨이브 슬로우 모션 킬캠:**
  - 7웨이브의 마지막 좀비를 처치하는 순간 게임 속도가 0.15배속으로 느려지며 극적인 연출이 시작됩니다.
  - 메인 카메라가 플레이어 어깨 너머 시점(뒤로 4m, 높이 3.5m)으로 2.5초간 부드럽게 줌인(Zoom-in)하며 회전하여 승리의 순간을 포착합니다.
  - 연출 종료 후 4초(실제 시간 기준) 뒤 자동으로 `Ending Scene`으로 장면이 전환됩니다.
- **최종 웨이브 승리 조건:**
  - 1~6 웨이브는 큰 좀비만 처치하면 넘어가지만, 7웨이브는 분열된 모든 작은 좀비까지 완벽히 소탕해야 엔딩 연출이 시작되도록 로직을 정교화했습니다.
- **Ending Scene 자동 시작 전환:**
  - `Ending Scene` 로드 후 타임라인 영상(`EndingDirectorTimeline`) 재생이 종료되면, 사용자가 수동으로 버튼을 누르지 않아도 시작 화면(`Intro Scene`)으로 자동 전환되도록 `IntroSceneController`에 `autoTransitionOnTimelineEnd` 제어 옵션을 추가하고 씬 내 인스펙터 설정을 활성화했습니다.
  - **무한 반복 루프 버그 수정:** 엔딩 씬의 `PlayableDirector` 컴포넌트의 `m_WrapMode`가 `1 (Loop)`로 잘못 지정되어 있어 영상이 무한 반복되고 종료 감지(`stopped`) 이벤트가 작동하지 않던 원인을 발견하여 `2 (None)`로 수정하였습니다.
- **인트로 씬 타임라인 시작 프레임 점프 (1200 프레임) & 블랙아웃 전환 연출:**
  - 엔딩 씬 재생 완료 시 타임라인의 원래 페이드아웃(블랙아웃) 연출이 정상 작동하도록 보장하기 위해, 엔딩 씬에서는 `FadeImg` 활성화 차단 및 수정 코드를 적용하지 않도록 씬 이름 기반 필터링을 도입했습니다. 이를 통해 타임라인 연출로 화면이 완전히 검은색이 되는 것을 인트로 씬이 로드되기 전에 온전히 보여주게 됩니다.
  - 인트로 씬으로 전환되면 타임라인이 **1200 프레임(60fps 기준 20초)** 지점부터 즉시 연출을 시작하며, 암전되어 있던 화면이 다시 1초간 부드럽게 밝아지는 **페이드인(Fade-in)** 연출을 동시 처리하여 자연스러운 화면 전환을 완성했습니다.
  - 전환 완료 후 UI 버튼들의 클릭 마우스 입력을 가로막지 않도록 `FadeImg`의 `raycastTarget` 차단 해제 및 오브젝트 비활성화 처리를 정밀 조율했습니다.

## 7. 협업 및 개발 가이드
- **UI 수정 시:** `UIManager`에 연결된 `SnowMan Cooldown Image` 등의 레퍼런스가 누락되지 않았는지 확인할 것.
- **머티리얼 설정:** `ZombieSpawner`와 `IntroSceneController` 오브젝트의 `Grass Material` 필드에 알맞은 머티리얼(`SnowGrass.mat`, `Grass.mat`)이 연결되어 있는지 확인할 것.
- **AI 베이킹:** 새로운 장애물 배치 시 반드시 `Navigation` 창에서 다시 `Bake` 해야 눈사람과 좀비가 길을 찾을 수 있음.
